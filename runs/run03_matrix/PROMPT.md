# run03 实验设计

## 与 run01 / run02 的唯一变量

| | run01 | run02 | run03 |
|---|---|---|---|
| 输入材料 | 模块源码 | 规格说明 | **规格说明（同 run02）** |
| Prompt 策略 | 「尽可能全面覆盖」 | 「尽可能全面覆盖」 | **「枚举特征维度并两两交叉」** |

**控制变量**：run03 用与 run02 完全相同的 SPEC.md，唯一改动是 prompt 里的覆盖策略。

## 盲测纪律（与前两轮一致）

- ❌ 不提 bug、不提 issue、不提 `ü`、不提「轻声」等任何指向性词
- ❌ 不给实现源码，禁止联网检索该项目，禁止看 git 历史
- ✅ 只给 SPEC.md
- ✅ 唯一新增的是**方法论指令**：要求枚举维度、构造笛卡尔积

## 判定

命中 = 生成的用例里存在「ü/v 韵母 + 轻声 5」的组合输入，且断言的是规范正确值。
